/*
NOTION:
The main entry point is not used, since all components are in subentry points,
however,
ng-packagr does not accept an empty entry point.
As a workaround, export a dummy variable like a constant with your library name.
*/

export const CARAVAN_UI_KIT = "@caravancoop/ui-kit";
